export enum Env {
    Dev = "development",
    Staging = "staging",
    Production = "production"
}

export enum Platform {
    Any = 0,
    Ios = 1,
    Android = 2,
    Web = 3
}

export enum EmailStatus {
    Pending = 0,
    Accepted = 1
}
